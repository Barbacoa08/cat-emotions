export const SignIn = () => {
  return (
    <div data-testid="SignIn-root">
      TODO: redirect here if user is not signed-in already. Put in
      google+github+FB signin options
    </div>
  );
};
